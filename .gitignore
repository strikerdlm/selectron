exports/
node_modules/
dist/
.env
.env.local
*.log
.DS_Store
.idea/
.vscode/

# TypeScript / Vite build artifacts
*.tsbuildinfo
vite.config.d.ts
vite.config.js
vitest.config.d.ts
vitest.config.js

# Playwright MCP scratch (browser snapshots/console logs)
.playwright-mcp/

# Playwright test runner artifacts (per-run results, traces)
test-results/

# Ad-hoc screenshots left at repo root by Playwright runs
/*.png

# PyMC MCMC checkpoints — multi-MB binary; commit only priors.json
notebooks/*.nc
notebooks/.ipynb_checkpoints/

# Python offline pipeline
python/.venv/
python/outputs/
python/src/*.egg-info/
__pycache__/

# Repo-root outputs (fitted priors, batch reports)
outputs/

# Graphify skill generated artifacts
research/graphify-out/
# Manuscript and journal-submission content lives only in selectron_private.
paper/*
!paper/REPRODUCIBILITY_LOCK.json
Review*.md

